import React, {useEffect} from 'react';
import styles from './App.module.less'

function App() {
  return (
    <div className={styles.same}>
      <span className={styles.title}>This is a single-spa example where you can write the UI interface you want</span>
    </div>
  );
}

export default App;
